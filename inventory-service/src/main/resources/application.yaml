server:
  port: 8882
  
grpc:
  server:
    port: 9992
  
spring:
  profiles:
    active: @project.build.type@
  application:
    name: inventory-service

info:
  app:
    name: @project.name@
    description: @project.description@
    version: @project.version@
    encoding: @project.build.sourceEncoding@
    java:
      version: @java.version@